# Top Contained + Bottom Stats Grid Collection

Layout keys:

- widget_top_contained
- widget_bottom_collection
- add_widget_preview_contained

All fields in this template are mapped to dynamic data keys so nothing is static.
